package com.dzaky.core_ui.ui

import androidx.compose.material3.lightColorScheme

val WeatherColorScheme = lightColorScheme(
    primary = Black,
    secondary = DarkMediumGray,
    tertiary = DarkMediumGray,
    surface = LightGray,
    onSurface = Silver,
    background = White,
    onBackground = Black
)